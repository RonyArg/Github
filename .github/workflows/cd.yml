name: CD

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

  push:
    branches:
      - main

  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-prod:
    name: Validate production secret
    runs-on: self-hosted

    steps:
      - name: Validate PROD_TOKEN
        shell: pwsh
        env:
          PROD_TOKEN: ${{ secrets.PROD_TOKEN }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:LOCAL_PROD_TOKEN)) {
            Write-Output "LOCAL_PROD_TOKEN not set in the environment"
            exit 1
          }

          if ($env:PROD_TOKEN -ne $env:LOCAL_PROD_TOKEN) {
            Write-Output "the production token is invalid"
            exit 1
          }

          Write-Output "the production token is valid"

  update-readme:
    name: Bump application version
    runs-on: ubuntu-latest
    needs: validate-prod

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Increment application version
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          CURRENT_VERSION=$(grep -m1 -oE 'AppVersion-[0-9]+' README.md | grep -oE '[0-9]+$')

          if [ -z "$CURRENT_VERSION" ]; then
            echo "No yet found AppVersion in README.md"
            exit 1
          fi

          NEXT_VERSION=$((CURRENT_VERSION + 1))
          TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M:%S UTC')

          sed -i "0,/^[[:space:]]*AppVersion-[0-9].*$/s//AppVersion-${NEXT_VERSION} - ${TIMESTAMP}/" README.md

          git add README.md
          git commit -m "bump application version to AppVersion-${NEXT_VERSION}"
          git push origin "HEAD:${{ github.head_ref || github.ref_name }}"
